#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    if(a>b)
    {
        printf("the a is greater");
    }
    else if(a==b)
    {
        printf("The number is equal");
    }
    else
    {
        printf("B is greater");
    }
}
